---
export const prerender = false;

import RegistrySearch from '../components/RegistrySearch.astro';
import { normalizeSearchQuery } from '../lib/format.ts';

const query = normalizeSearchQuery(Astro.url.searchParams.get('q'));
---

<RegistrySearch slug="rubygems" query={query} />
