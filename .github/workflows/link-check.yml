name: Link Check

on:
  schedule:
    - cron: "0 14 * * 1" # every Monday at 14:00 UTC
  workflow_dispatch:

permissions: {}

concurrency:
  group: "link-check"
  cancel-in-progress: true

jobs:
  lychee:
    name: Check links
    permissions:
      contents: read
    uses: starhaven-io/.github/.github/workflows/reusable-link-check.yml@5516bc8a2030979985d2f69e6017100992444cfb # v2026.09.06.6
    with:
      args: "--exclude '^http://localhost:4321/?$' README.md DATA_SOURCES.md seed/README.md"
      build-site: false
      site-directory: ""
      authenticated-github: true
